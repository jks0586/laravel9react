import React from 'react';
import { Link } from '@inertiajs/inertia-react';

export default function AdminLeft({ href, active, children }) {
    return (
        <div className='active'> this is left menu to craete left section</div>
    );
}